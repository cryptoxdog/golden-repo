# Optional semantic review provider key.
# Deterministic review does not require this to be set.
ANTHROPIC_API_KEY=

# Local output directory used by Makefile review targets.
REVIEW_ARTIFACT_DIR=.artifacts/review

# Optional override for evaluation replay results.
EVAL_OUTPUT=.artifacts/evals/eval_results.json
