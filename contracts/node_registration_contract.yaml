version: "1.0"
schema_name: node_registration_contract

required_fields:
  - node_name
  - node_version
  - supported_actions
  - health_endpoint

optional_fields:
  - description
  - tags
  - max_concurrent_executions
